# 墨西哥 RFC 税号获取指南（跨境自发货卖家版）

> 一句话结论：**纯跨境自发货直邮（买家=进口商）在法律上不强制要求 RFC**，也避开了 CBT 海外仓"无 RFC"被扣的 36% 双重税。但 **Mercado Libre 自 2024-10-14 起，对未提供 RFC 的卖家按其成交价预扣 16% IVA**；2026 新法进一步要求平台对跨境交易预扣。所以要不要办 RFC，取决于你的**体量**和**对那 16% 的容忍度**。

---

## 一、RFC 是什么，为什么和你有关

- **RFC（Registro Federal de Contribuyentes）** = 墨西哥税务局(SAT)发的纳税人识别号，相当于"税务身份证"。
- 平台（Amazon MX / Mercado Libre）用 RFC 决定是否预扣税、能否开票(CFDI)。
- **对跨境自发货**：传统直邮模式下买家是进口商，关税由买家承担，卖家侧本不强制 RFC。
- **但 ML 的实操**：无 RFC → 平台直接预扣 **16% IVA**（注意：ML 只扣 IVA，不像亚马逊那样扣到 36%）。这 16% 是你实际少赚的钱，不是封号（除非你设了墨西哥仓却无 RFC，那才会被下架/清关卡）。

**工具里的对应设置**：利润引擎 `税务模式` 四档——
- `跨境自发货直邮（0%）`：最乐观，假设无平台预扣（适合早期小体量试水）。
- `CBT 跨境店 + 有 RFC（16%）` / `CBT 跨境店 + 无 RFC（36%）`：海外仓模式。
- `墨西哥本土店（10.5%）`：在墨注册了本地公司。

> 建议：先用 `direct_import(0%)` 跑数据；等日销起来、发现 ML 实际在扣你 16%，就走下面任一路径注册 RFC，把模式切到 `cbt_with_rfc(16%)` 并保持合规。

---

## 二、三种获取路径对比

### 路径 A：委托墨西哥本地税务代理（Procurador Autorizado，SAT 认证）— 主流选择
- **适用**：未在墨设公司、但需要 RFC 入驻/开票的外资卖家（试水首选）。
- **流程**：选有 SAT 资质的代理 → 准备材料 → 签署西语公证授权书（指定代理为你的 Representante Fiscal）→ 代理向 SAT 提交 → 拿到 RFC（+ 可选 e.firma）。
- **周期**：约 12–15 天。
- **费用**（参考，以实时报价为准）：
  - 单次注册（不含实体）约 **$200–400**；
  - 含 e.firma 电子签名 + SAT 门户 + 税务邮箱约 **$400–600**；
  - 全包（公司+RFC+发票系统+咨询）约 **$800–1500**。
- **注意**：务必确认代理有 **SAT 认证资质**；e.firma 电子签名要一并办（否则无法开票/在线报税）；要 SAT 官方回执。

### 路径 B：第三方合规服务商一站式（中文服务）— 中小卖家友好
- **代表**：沙之星跨境、PingPong、万里汇 等已与 SAT 认证代理合作的服务商。
- **优点**：中文界面 + 双语支持，流程省心。
- **周期**：约 10–14 天。
- **费用**：打包价约 **人民币 5000 元以内**（常见）。
- **关键核验**：务必确认服务商有 **SAT 授权编号（Clave del Procurador）**，避免"黑代理"导致申请无效、甚至被列入黑名单。

### 路径 C：在墨西哥设立本地公司（S.A. de C.V.）后申请 — 长期/设仓才选
- **适用**：计划长期经营、设 FBA/第三方仓、对接大客户。
- **流程**：注册公司（章程公证+商业登记）→ 向 SAT 申请 RFC → 办 e.firma → 配 CFDI 发票系统 → 注 IVA/ISR/社保。
- **费用**：公司注册+RFC+发票系统合计约 **$800–1500**（不含后续会计）。
- **周期**：数周。

---

## 三、通用所需材料

- 公司营业执照 / 公司章程（中文需**西语公证 + 海牙认证**）。
- 法人护照。
- 公司地址证明（可用正规虚拟办公室合同+缴费单作为税务住所）。
- 授权委托书（西班牙文公证），授权墨西哥方为 Representante Fiscal。
- 部分情况需要 **CURP**（可走 SAT"外国人 RFC 登记"通道用临时 CURP 启动）。

---

## 四、e.firma 电子签名（必须办）

- 是开具 CFDI 电子发票、在线月度申报、给报关行电子授权的**基础**。
- 标准流程需本人/授权代表到 SAT 现场采集；**可委托持 POA 的代理远程办理**（节省出差）。
- 建议：办 RFC 的同时直接申请 e.firma，避免二次跑流程。

---

## 五、风险与红线

- ❌ 不使用**虚假地址 / 伪造文件**注册：一经查实列入 SAT 黑名单，**5 年内禁止申请**，并可能影响其他拉美国家税务准入。
- ❌ 不使用"黑代理"（无 SAT 授权编号）。
- ✅ 选有资质、能出 SAT 官方回执、有真实案例与合同的代理。
- ✅ 注册后你成为墨西哥税务系统纳税人，需按时申报（可让代理代管月度申报/开票）。

---

## 六、实操建议（给本工具用户）

1. **起步期（日销小）**：工具选 `direct_import(0%)`，先跑通"抓爆款→货源→标题→配图→上架"全流程，验证选品。
2. **起量后**：若发现 ML 实际在扣你 16% IVA，走**路径 A 或 B** 注册 RFC（预算 ¥3000–5000 / $400–700），把工具切到 `cbt_with_rfc(16%)`，既止住不确定性也向合规过渡。
3. **打算设墨仓 / 大客户**：走**路径 C** 注册本地公司。
4. 无论哪种，保留 SAT 回执与代理合同，作为合规凭证。

---

### 参考来源（公开资料，2026-07 复核）
- 沙之星跨境《墨西哥 RFC 收费标准/注册流程》
- 10100 百科《墨西哥 RFC 税号注册三种路径》
- 卖家经验帖（MJZJ）：RFC 登记 + e.firma + CFDI 开票实操

> 注：税务规则随 SAT 政策变动（2026 新《联邦收入法》已生效），注册前请以 SAT 官网与代理最新口径为准。
