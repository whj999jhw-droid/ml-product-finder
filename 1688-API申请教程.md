# 1688 开放平台 API 申请教程（美客多跟卖工具配套）

本教程面向想用「自动图搜/找同款」功能的用户。配置好后，「货源匹配」页的 **API图搜** 按钮会调用 1688 官方接口，按商品图或中文品名自动找同款货源（进货价 / MOQ / 供应商 / 主图），比免密钥模式更准、更稳定。

---

## 一、前置条件

1. 一个 **1688 / 阿里巴巴中国账号**（个人/企业均可），并完成**实名认证**。
2. 你打算跟卖的商品，在 1688 上能搜到对应货源（这是前提）。

---

## 二、创建应用，拿到 App Key / App Secret

1. 浏览器打开 **[开放平台 open.1688.com](https://open.1688.com)**，用 1688 账号登录。
2. 进入「**开发者中心 → 我的应用**」，点「**创建应用**」。
3. 应用类型建议选 **「自用型应用」**（你是给自己店铺/工具用的）；填应用名称（如 `ML跟卖工具`）、应用描述、回调地址可先填 `https://localhost`（后面取 token 用）。
4. 创建成功后，在应用详情里能看到 **App Key** 和 **App Secret**，先复制保存好（Secret 只显示一次，务必存下）。

![应用详情](建议截图：开发者中心-应用详情-AppKey/AppSecret 区域)

---

## 三、申请 API 权限

「自动图搜」需要以下接口权限（在「**应用 → API 权限管理**」里申请）：

| 用途 | API 名称 | 说明 |
|------|----------|------|
| 关键词搜货 | `alibaba.product.search` | 拿中文品名找同款 |
| 以图搜货 | `alibaba.product.image.search` | 拿竞品图/货源图反搜（最推荐） |

- 部分接口需要**提交使用场景说明**或**资质审核**，按页面提示填写即可（说明写"跨境选品比价/货源匹配"即可）。
- 若提示需购买调用包，按平台指引开通（基础搜索通常免费额度足够个人使用）。
- 以图搜货（image.search）可能需要单独**开通**或购买，请按开放平台当前规则操作。

![权限申请](建议截图：API权限管理-勾选 product.search / image.search)

---

## 四、获取 access_token

工具用 `access_token` 代表你的店铺调用接口。1688 采用 OAuth 授权码模式：

1. 构造授权链接（把 `APPKEY` 和 `REDIRECT_URI` 换成你自己的）：
   ```
   https://auth.1688.com/oauth/authorize?client_id=APPKEY&redirect_uri=https://localhost&site=1688
   ```
2. 浏览器打开该链接，登录并**确认授权**，页面会跳回 `redirect_uri?code=XXXXXXXX`。
3. 用这个 `code` 换 `access_token`（后端已封装好签名，工具会自动处理；你只需把最终 token 填进环境变量）。
   - 自助方式：在开发者中心的「**我的应用 → 获取 access_token**」工具里，贴入 code 即可拿到 token。
4. `access_token` 有**有效期**（通常几十天到一年不等），过期后用 `refresh_token` 刷新，或直接重新走一遍授权。

![授权](建议截图：授权确认页 与 token 工具页)

---

## 五、配置到工具（环境变量）

在工具的运行环境里设置以下三个变量（二选一）：

- **本地运行**：在项目根目录新建 `.env` 文件，写入：
  ```
  ML_1688_APPKEY=你的appkey
  ML_1688_SECRET=你的secret
  ML_1688_TOKEN=你的token
  ```
  然后重启后端（`npm run server` 或双击启动器）。
- **Render 等云端部署**：在对应服务的 **Environment** 里添加同名变量，保存后重新部署。

> 变量名必须完全一致：`ML_1688_APPKEY` / `ML_1688_SECRET` / `ML_1688_TOKEN`。

---

## 六、在工具里使用

1. 打开「**货源匹配 & 利润测算（M2）**」页。
2. 每行点「**API图搜**」：工具会用该行商品图（或标题）调用 1688 接口，自动回填 **进货价 / 供应商 / 货源链接 / 货源主图**。
3. 拿到货源主图后，点顶部「**批量配图**」：工具会把 1688 货源图（可选加水印/你的店铺名）上传到美客多，生成你自己的 Listing 图。
4. 点「**一键生成标题**」→「**一键上架**」完成全流程。

---

## 七、常见问题

| 现象 | 原因 / 解决 |
|------|------------|
| 提示"未配置 1688 密钥" | 没设 `ML_1688_*` 环境变量或后端未重启。检查 `.env` 并重启。 |
| 返回签名错误（sign / 403） | `App Secret` 填错或 param2 签名拼接顺序不对。工具已按标准实现，核对 Secret 是否完整复制。 |
| 返回"权限不足 / 接口未开通" | 对应 API（如 image.search）未申请或审核未过，回第三步补齐权限。 |
| token 过期 | 重新走第四步授权拿新 token，更新环境变量。 |
| 以图搜货无结果 | 图太特殊/1688 无同款；改用「1688找同款」关键词模式（中文品名）。 |
| 调用频率限制 | 1688 有 QPS 限制，工具已做单条间隔；批量时放慢节奏或错峰。 |

---

## 八、费用说明（参考，以平台实时规则为准）

- 开放平台**注册/创建应用**本身免费。
- 搜索类接口通常有**免费额度**，超出按调用计费或需购买 API 包。
- 以图搜货（image.search）可能需单独开通/付费，请按开放平台当前定价。
- 相比"免密钥浏览器模式"（常被 1688 验证墙拦截、不准），开放平台 API 更稳定、合规、可批量，值得配置。

---

> 合规提醒：1688 货源图是**供应商提供给你作为买家/分销商使用的图**，可加水印做成自己的 Listing 图；但**绝不要**把从美客多抓到的竞品原图直接搬用到你的 Listing（侵权+重图封号风险）。
