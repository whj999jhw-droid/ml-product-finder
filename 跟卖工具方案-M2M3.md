# 跟卖工具方案 · M2/M3 接口设计（备用）

> 目标：做一款「采集美客多爆款 → 跟卖爆款」的合规工具，面向国内无货源跨境卖家（非本土店、非 Full 店）。
> 本文是 M1（采集爆款·住宅代理放量）之后的**备用设计方案**，待 M1 跑通后按需实现。

---

## 0. 合规红线（必须守住）

✅ 合规版（本工具只做这些）：
- 采集爆款（M1 已做）
- 自己找 1688 同源货源（M2）
- **用你自己的 Listing 合规上架**：自己写标题/描述、自己拍或获授权图片、自己定价、自己设物流（M3）
- 跟价（拉竞品价 PATCH 自己商品价格）、侵权/品牌预检（M4）

❌ 不做（违反美客多规则、封号 + 侵权索赔风险，坚决不实现）：
- 复制竞品**销量**（伪造 sold_quantity）
- 复制竞品**真实评价/评论**
- 原样盗用他人商品图、品牌词、商标

---

## 1. 模块总览

| 模块 | 功能 | 状态 | 依赖 |
|------|------|------|------|
| M1 采集爆款 | ML API + 住宅代理翻页，过滤非 Full/本土/全新/≤$X | ✅ 已实现（本阶段） | 住宅代理 |
| M2 找货源+算利润 | 1688 图搜同源、拿进货价、算净利润 | 🆕 本文 | 1688 接口/图搜 |
| M3 合规上架 | 用卖家 write token 调 `POST /items` 自建 Listing | 🆕 本文 | 卖家 OAuth(write)、图床 |
| M4 运营 | 跟价(PATCH 价格)、侵权预检、库存/订单同步 | 🆕 本文 | M3 + 货源 API |

落地顺序建议：**M1 → M2 → M3 → M4**。

---

## 2. M2：找货源 + 利润测算

### 2.1 输入
M1 输出的每条爆款：`title / thumbnail / price(本地) / priceUSD / permalink / category`。

### 2.2 1688 图搜同源
- **方式 A（推荐）**：1688 开放平台「以图搜货」能力（`https://open.1688.com` 的 image-search / 商品推荐 API），用商品主图 URL 或二进制调搜索，返回同源/相似款列表。
- **方式 B（兜底）**：用商品标题关键词在 1688 搜索 API（`https://p4psearch.1688.com/...` 或开放平台 `alchemy.search`）搜同源。
- **反爬注意**：1688 有登录态/滑块校验，生产环境建议用开放平台 API（需 1688 开发者账号）或带登录 Cookie 的请求，避免高频触发验证码。

### 2.3 输出字段（每条货源）
```
sourcePriceCNY      // 1688 进货价（人民币）
moq                 // 最小起定量
supplier           // 供应商/店铺
supplierLevel      // 实力商家/超级工厂等
shippingToMX_CNY   // 国内→墨西哥头程运费（按重量估算或物流接口）
sampleUrl          // 货源链接
```
> 汇率：用 ML 现有 `getExchangeRate(CNY→USD)` 思路，或固定 CNY→USD、再结合站点本地货币。

### 2.4 利润测算
```
landedCostCNY = sourcePriceCNY + shippingToMX_CNY
landedCostUSD = landedCostCNY / USDCNY
mlFeeUSD      = priceUSD * ML_COMMISSION   // ML 佣金约 11%~17%（按类目），可用固定 13% 预估
profitUSD     = priceUSD - landedCostUSD - mlFeeUSD - payoutFeeUSD
ROI           = profitUSD / landedCostUSD
```
- `ML_COMMISSION` 按类目查 `GET /sites/{site}/listing_exposure_prices` 或固定 0.13 估算。
- 仅当 `profitUSD > 0` 且 `ROI ≥ 阈值(如 0.2)` 的爆款进入「可跟卖」清单。
- Excel 导出增加列：货源价、头程运费、预估佣金、净利润、ROI、货源链接。

### 2.5 反爬/稳定性
- 图搜限频（每商品间隔 1–2s），失败重试 + 兜底关键词搜索。
- 结果缓存（同一商品 24h 内不重复搜）。

---

## 3. M3：合规上架到美客多

### 3.1 卖家授权（write token）
- 复用现有 OAuth 基础设施：`/api/ml/oauth/auth-url`（authorization_code + PKCE + 公网隧道回调）、`/api/ml/oauth/exchange`、`/api/ml/oauth/refresh`。
- scope 需含 `write`（现有授权 URL 已含 `read,offline_access`，需补 `write`）。
- 该 token 是**你店铺**的授权，不是 app 级 `client_credentials`（只读）。前端加一步「授权我的店铺」。

### 3.2 创建 Listing：`POST /items`
必填/常用字段：
```
{
  title: string,                 // 你自己撰写（含关键词，不抄竞品）
  category_id: string,           // 来自 M1 的 categoryId
  price: number,                 // 你的定价（建议 = 竞品价 ± 策略，留利润）
  currency_id: string,           // 站点货币 MLM=MXN 等
  available_quantity: number,
  buying_mode: 'buy_it_now',
  listing_type_id: 'gold_pro' | 'silver',   // 普通/优质，非 Full
  description: { plain_text: string },       // 自己写
  pictures: [ { source: <公开图片URL> }, ... ],  // 见 3.3
  shipping: { mode: 'custom', local_pick_up:false, free_shipping:false,
             dimensions: { weight, height, width, length } },  // 非 Full：自选物流
  attributes: [ { id:'BRAND', value_name:'你的品牌或 Generic' }, ... ]
}
```
- **非 Full 店**：`shipping.mode='custom'`（自己设运费），不经 `fulfillment`。
- 返回 `item_id` + `permalink`（你的商品页）。

### 3.3 图片托管（关键点）
ML 的 `pictures[].source` 必须是**公网可访问的 URL**，ML 会主动抓取。两种做法：
- **方案 1（推荐）**：先把图片上传到你自己的图床/CDN（或用 ML 的 `POST /pictures` 上传接口拿到 ML 托管 URL），再引用。
- **方案 2**：引用 1688/货源公开图（仅当你获授权或图片无版权问题；否则用你自有图）。
- ⚠️ 严禁直接盗用竞品 Listing 原图（侵权）。建议：用货源图或自行拍摄/合成图。

### 3.4 跟价（M4 一部分，先列此处）
- 定时 `GET /sites/{site}/search?q=<你的商品关键词>` 取竞品价（含 M1 的 search 逻辑）。
- 当竞品降价，用 `PUT /items/{item_id}`（或 `POST /items/{item_id}/price`）PATCH 自己商品价格，保持竞争力。
- 频率：每 SKU 每 30–60 分钟一次，避免触发限流。

### 3.5 侵权预检（上架前）
- 维护品牌黑名单（Nike/Apple/Disney 等）+ 商标词；上架前正则校验 title/attributes/图片。
- 命中则跳过该产品并提示，避免侵权封店。

---

## 4. M4：运营管理（无货源闭环）

- **库存同步**：你店铺出单后，自动向 1688/货源下单（需货源下单 API 或代发服务商）。
- **订单同步**：拉 ML `GET /orders`（卖家 token）映射到货源订单，回传运单号 `POST /orders/{id}/shipments`。
- **利润看板**：汇总已售、成本、净利润。
- 注：M4 依赖货源侧代发能力，属进阶模块，建议 M1–M3 跑通后再做。

---

## 5. 技术与风险小结

- **住宅代理仍是 M2/M3 采集端的命脉**：1688 与 ML 搜索都需地理/住宅 IP，沿用 M1 的代理框架。
- **速率限制**：ML 带 token ≈ 1500 次/分；1688 开放平台按配额。均加延时 + 重试。
- **合规**：M3 只建你自己内容的 Listing；任何「复制销量/评论/他人图」功能一律不做。
- **账号安全**：建议小批量灰度上架，观察店铺健康度再放量。

---

## 6. 待确认（实现前）
1. 是否有 1688 开放平台账号 / 货源代发渠道？决定 M2/M4 用 API 还是人工。
2. 卖家的美客多店铺是否已在「非 Full、跨境」模式下开通？决定 M3 shipping 设置。
3. 上架是否需要自动跟价（M4），还是先手工运营？
