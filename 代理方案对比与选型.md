# 美客多数据抓取：代理方案对比与选型（2026-08 实测）

## 一、数据量的硬约束（中国 IP）

| 端点 | 是否需要 token | 中国直连结果 | 数据量 |
|------|------|------|------|
| `/highlights/{site}/category/{cat}` | 需要 | ✅ 200（2026-08-01 实测） | 每类目**固定 ~20 条** Best Seller，无分页（API 级上限） |
| `/search?q=...` | 不需要（公开端点） | ❌ **403 forbidden**（WAF 按 IP 地理封锁，带 token 也绕不过，2026-08-01 实测） | 可翻页，每页 50，可达数百/类 |
| `/items/{id}`、`/products/{id}` | 部分需要 | 正常 | 单品详情 |

**结论：**
- 不代理、只靠 highlights：每类 **20 条封顶**（API 硬限制，无法绕过）。
- **免费放大法（重点）**：开启工具里的「展开子分类」——每个子分类各自返回自己的 ~20 条 Best Seller，深度遍历可**免费**拿到 100–300+ 条/大类，零成本。这是「不买代理也能拿更多」的正解。
- 想用 `/search` 翻页拿几百条/类：必须解锁地理封锁 → 住宅代理，或把后端跑在海外 IP。

## 二、更便宜的代理选项（2026 真实行情）

> ⚠️ **必须买「住宅代理 residential，且 geo 匹配目标国（MX/BR/CL/CO）」**。
> 数据中心代理（Webshare 等 $0.03–0.5/GB）会被美客多风控识别并封/CAPTCHA，**不要买来抓 /search**。

| 服务商 | 住宅代理价格 | 最低消费 | 说明 |
|--------|------|------|------|
| **PacketStream** | $1–3/GB | 无（按量） | 市场最便宜住宅代理；成功率 ~82%，对非高防目标够用 |
| **DataImpulse / GProxy** | ~$1/GB | 无 | 极便宜按量，适合试水 |
| **IPRoyal** | $1.75–5/GB | 无 | 流量**不过期**，32M 池，可 geo 选国，预算友好 |
| **Decodo（原 Smartproxy）** | $2–7/GB | PAYG $4/GB / 订阅 $50 起 | 最稳（99.86% 成功率），性价比均衡，推荐主力 |
| Bright Data / Oxylabs | $8–10/GB | $500 / $99 起 | 企业级，个人/小团队偏贵，非必需 |

**成本估算**：一次全量抓取（54 大类 × 4 国，翻页到 ~100/类）约 0.5–1GB 流量 → 住宅代理约 **$1–7（约 ¥7–50）**，非常便宜。比「按条买数据」划算得多。

## 三、比代理更便宜的备选：海外 VPS（零代理费）

把后端（Node）跑在**美国/拉美廉价 VPS**（如 Hetzner/DigitalOcean 美区或拉美节点，约 $5/月），从那里调 `/search`。若该 IP 不被 WAF 封，则**完全不用代理**。

- **风险**：部分云 ASN 也被封。此前 Cloudflare Worker 的 DC IP 实测 /search 403；普通 VPS 是否放行需实测（不同机房 ASN 差异大）。
- 现有架构已支持 Render（AWS）部署，但此前测试 /search 仍 403，故 AWS 不一定可行；可试其他机房。
- **建议**：先开 1 个月 $5 VPS 实测 `/search`，通了就省下所有代理费；不通再回退住宅代理。

## 四、合规说明

本工具走的是**美客多官方 API**（/highlights、/search、/items），属于平台许可的数据获取方式，与 HTML 爬虫是两回事。代理仅用于解锁「中国 IP 地理封锁」，不改变数据来源的合法性。注意：官方 Developer Terms 反对对网站做 HTML 爬虫；我们用 API 不受影响。

## 五、当前建议路径

1. **日常先用「直连 + 展开子分类」免费拿量**——对选品（找爆款跟卖）通常已足够。
2. **若要长尾深度（几百/类）**：先试 $5 海外 VPS；不行再上 PacketStream / IPRoyal 住宅代理（geo=MX/BR/CL/CO）。
3. 旧 Decodo 凭证已失效（407），已清空；如要复用 Decodo，需续费换新凭证后重新填。
